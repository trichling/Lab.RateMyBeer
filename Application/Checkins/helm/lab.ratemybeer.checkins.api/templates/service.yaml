apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab.ratemybeer.checkins.api.fullname" . }}
  labels:
    {{- include "lab.ratemybeer.checkins.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab.ratemybeer.checkins.api.selectorLabels" . | nindent 4 }}
