apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab.ratemybeer.frontend.api.fullname" . }}
  labels:
    {{- include "lab.ratemybeer.frontend.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab.ratemybeer.frontend.api.selectorLabels" . | nindent 4 }}
